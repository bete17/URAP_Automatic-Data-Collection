How to use it:
  have a txt file with the name formatted as f"{gvkey}_{fyear}_item7.txt"
  then run main.py with ollama open, you need to download GPT-OSS:20b model locally for it to run 

Potential issues:
  1. URL, still havent figure that out
